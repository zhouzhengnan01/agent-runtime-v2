{
  "name": "emoji",
  "description": "emoji.",
  "output_kind": "markdown",
  "generation": true,
  "routing": {
    "summary": "Use for experiment YAML records, result summaries, compare-experiment tables, and post-training retrospectives.",
    "keywords": [
      "emoji"
    ],
    "examples": [
      "输出里带有emoji"
    ]
  },
  "quality_template": [
    "experiment_id",
    "parameters",
    "machine",
    "dataset",
    "status",
    "metrics",
    "notes"
  ],
  "input_schema": {
    "type": "object",
    "required": [
      "skill_name"
    ],
    "properties": {
      "skill_name": {
        "const": "emoji"
      },
      "experiment_id": {
        "type": "string"
      },
      "model": {
        "type": "string"
      },
      "dataset": {
        "type": "string"
      },
      "status": {
        "type": "string"
      }
    },
    "additionalProperties": true
  },
  "output_schema": {
    "type": "object",
    "required": [
      "artifacts"
    ],
    "properties": {
      "artifacts": {
        "type": "array"
      }
    },
    "additionalProperties": true
  },
  "sandbox": {
    "enabled": false,
    "profile": null,
    "request_schema_version": "skill-run.v1",
    "fallback_to_local": true
  },
  "model_tags": [
    "chat"
  ]
}
