{
  "type": "object",
  "required": ["decision", "category", "confidence", "evidence_mode"],
  "properties": {
    "decision": { "type": "string" },
    "category": { "type": "string" },
    "confidence": { "type": "number", "minimum": 0, "maximum": 1 },
    "evidence_mode": { "type": "string", "enum": ["text_only", "visual_or_structured"] },
    "reason": { "type": "string" }
  },
  "additionalProperties": true
}
