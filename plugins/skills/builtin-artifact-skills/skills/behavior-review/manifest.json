{
  "name": "behavior-review",
  "description": "Evidence-first second-pass review for behavior alerts, producing review conclusion, evidence gaps, handling actions, and manual review recommendation.",
  "output_kind": "json",
  "generation": false,
  "routing": {
    "summary": "Use for AI复判, 人工复核, 二次审核, 告警审核 after behavior detection or alarm events.",
    "keywords": ["复判", "复核", "二次审核", "人工审核", "告警审核", "AI复判", "review"],
    "examples": ["对这个翻越告警做 AI 复判", "复核老人摔倒事件是否需要升级处理"]
  },
  "quality_template": ["evidence_first", "review_decision", "evidence_gaps", "actions"],
  "input_schema": {
    "type": "object",
    "required": ["skill_name", "text_rule_candidates", "has_visual_evidence"],
    "properties": {
      "skill_name": { "const": "behavior-review" },
      "event_id": { "type": "string" },
      "original_decision": { "type": "string" },
      "text_rule_candidates": { "type": "array", "items": { "type": "string" } },
      "has_visual_evidence": { "type": "boolean" },
      "attachments": { "type": "array", "items": { "type": "object", "additionalProperties": true } }
    },
    "additionalProperties": true
  },
  "output_schema": {
    "type": "object",
    "required": ["review_decision", "risk_level", "evidence_mode", "actions"],
    "properties": {
      "review_decision": { "type": "string" },
      "risk_level": { "type": "string" },
      "evidence_mode": { "type": "string" },
      "actions": { "type": "array", "items": { "type": "string" } }
    },
    "additionalProperties": true
  },
  "sandbox": { "enabled": false, "profile": null, "request_schema_version": "skill-run.v1", "fallback_to_local": true }
}
