# Algorithm Engineer Skill Suite

This plugin turns a computer vision model iteration request into structured artifacts:
research shortlist, dataset audit plan, candidate model plan, GPU training plan, evaluation report,
deployment review, and experiment ledger.

The skills are intentionally conservative. They produce executable plans, commands, checklists, and records,
but do not stop remote services or launch long GPU jobs by themselves.
